class intervalsFloat {

	basic() {
		def a = 1.5;
		def b = -0.25;
		def c = a + b;
		def d = a * b;
		def e = a / b;
		def f = -a;
	}

	mixed() {
		def i = 3;
		def x = 0.5;
		def y = i + x;
		def z = x * 2;
	}

	loop() {
		def x = 0.0;
		while (x < 1.0) {
			x = x + 0.1;
		}
		return x;
	}
}
