// Generated by Cursor
// Scenario: graphics framebuffer byte sum overflows when blending layers

class FramebufferBlender {

	blendChannel(a, b) {
		return a + b;
	}

	stackLayers(base, layer1, layer2, layer3) {
		def acc = base + layer1;
		acc = acc + layer2;
		acc = acc + layer3;
		return acc;
	}

	multiplyBrightness(pixel, factor) {
		return pixel * factor;
	}
}
