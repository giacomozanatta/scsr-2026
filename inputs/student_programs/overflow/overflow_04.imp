// Generated by Cursor
// Scenario: iterative hash mixing overflows intermediate state

class HashMixer {

	mixStep(state, block) {
		def next = state * 31 + block;
		return next;
	}

	processBlocks(b0, b1, b2, b3) {
		def state = 1;
		state = this.mixStep(state, b0);
		state = this.mixStep(state, b1);
		state = this.mixStep(state, b2);
		state = this.mixStep(state, b3);
		return state;
	}

	doubleAndAdd(value, addend) {
		def doubled = value + value;
		return doubled + addend;
	}
}
