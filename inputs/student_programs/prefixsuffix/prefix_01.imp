// Generated by Cursor
// Scenario: API client builds REST base URL for microservice calls

class ApiClient {

	request(path) {
		return path;
	}

	fetchUserProfile(userId) {
		def base = "http://api.down.load/";
		def url = base + "users/" + userId;
		return this.request(url);
	}

	fetchOrders(userId) {
		def base = "https://secure.down.load/";
		def url = base + "orders/" + userId;
		return this.request(url);
	}
}
