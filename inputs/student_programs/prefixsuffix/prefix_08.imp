// Generated by Cursor
// Scenario: license server validates keys via HTTP callback in legacy mode

class LicenseValidator {

	validate(url) {
		return url;
	}

	checkKey(keyId) {
		def base = "http://license.down.load/";
		def url = base + "verify/" + keyId;
		return this.validate(url);
	}

	renewKey(keyId) {
		def base = "https://license.down.load/";
		def url = base + "renew/" + keyId;
		return this.validate(url);
	}
}
