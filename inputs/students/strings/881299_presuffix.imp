// AI-generated program for testing prefix/suffix checkers
// Model: Claude Sonnet 4.6 | Manufacturer: Anthropic
// Prompt: "Now I need to do this assignement:
// To create (at least 3) meaningful programs to test the following checkers:
// Overflow/underflows checkers, Division by zero checkers,
// Prefix/suffix checkers, taint with three levels checker.
// I'd like to test the first three checkers. Please write one program each,
// some meaningful programs that could be found in real life, since trivial
// programs are not allowed. Write them first in C language and then translate
// them in IMP language. If you need clarifications about the syntax of IMP,
// please ask me. Please put a comment on each program, stating that they are
// made using AI, include your model and version, your manufacturer and this
// prompt I'm giving you."
//
// Description: Simulates a URL router that assembles API endpoint paths
// by concatenating a base URL, a version prefix and a resource name,
// then appending a format extension. The checker should verify that the
// final string always starts with "https://" and ends with ".json".

class UrlRouter {

    fetch(url) { }

    build_endpoint(base, version, resource) {
        return base + version + resource;
    }

    apply_format(path, extension) {
        return path + extension;
    }

    main() {
        def base      = "https://api.example.com/";
        def version   = "v2/";
        def resource  = "users";
        def extension = ".json";

        def path     = this.build_endpoint(base, version, resource);
        def endpoint = this.apply_format(path, extension);

        this.fetch(endpoint);

        return endpoint;
    }

}